package routes

import "github.com/zatrano/framework/v2/contracts"

var application contracts.App

// Use binds the application for self-registered route functions.
func Use(app contracts.App) {
	application = app
}

// Current returns the bound application.
func Current() contracts.App {
	return application
}
